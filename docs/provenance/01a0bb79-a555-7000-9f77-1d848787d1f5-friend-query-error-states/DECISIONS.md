- These changes come from seven reviews of "Fail a friend query when the server refuses it". Each time the user asked for every finding to be fixed, including the nits, and asked that no REQUIREMENTS.md change; the fourth, fifth and sixth asked for `web-client-default` to be filed in Linear instead of fixed. There is no REQUIREMENTS.md here because nothing in this work came from the user beyond those instructions.
- Finding `body-status`: the retry rule read `statusCode` out of the body `@hey-api/client-fetch` throws, which is a NestJS convention and not anything HTTP guarantees. A refusal shaped by anything else fell through to the retry branch and still cost four requests: measured against `serveApi`, four attempts each for a 400 bodied `{ message, errors }`, a 401 with an empty body, and a 401 answered in plain text. The first is what NestJS returns for an exception built from an object literal, which drops `statusCode` — `tasks.service.ts:276` and `actions.service.ts:1340` do that today — and the other two are what a proxy in front of the API answers with. `registerErrorStatus` puts `response.status` on the error from the client's error interceptor, the one place that still holds the response, so the rule reads a status that is always there. It is the first commit of the task, ahead of the rule that reads it, so no deploy has the rule trusting a field that can be missing. The provenance directory starts here.
- This was ALL-1111's second half, "worth doing at the same time" as the `throwOnError` sweep. It is split off because it preserves object bodies and adds the response status. Non-object bodies stay under `body` for logging, which `errorMessage` ignores so a proxy's HTML error page leaves the caller's fallback intact. The sweep stays in ALL-1111, since it changes what 79 call sites return.
- Registered from each app root and from `serveApi`, rather than from `createClientConfig`. That function runs while `createClient` is still building the client, so there is no `interceptors` to register on yet. `serveApi` takes it so a test sees the error every app ships; four assertions on the old shape took the status code, in `serveApi.test.ts` and `useBackfillTimeZone.test.ts`.
- `useDeleteComment` and `useLoadComments` pass `throwOnError: false` to hold on to the response their `refusalMessage` call reads the status off, and say so in a comment. The interceptor makes that unnecessary, and they are left alone: rewriting them is a second change, and neither is part of the friend queries this task covers.
- Finding `retry-on-refusal`: a query that throws gets React Query's default of three retries, and the web `QueryClient`s set no `retry`, so a 401 or 404 was retried three times even though it cannot succeed. Measured: four requests per query. On web each 401 also re-enters the refresh wrapper in `shared/lib/hey-api.ts`, so a profile page reading all five friend queries made about twenty requests and as many refresh attempts. `retryUnlessRefused` in `shared/lib/retryQuery.ts` now holds the rule, and the three `QueryClient`s take it, mobile keeping the two retries it already asked for. The rule covers every query in each app rather than these five, so it is its own commit, the one ALL-1112 asked for. It reads the `statusCode` the commit before it leaves on every error the client throws.
- Finding `retry-storm`: that commit now comes first, ahead of the one that makes the five friend queries throw, so no deploy sits in the window where a refused friend read costs four requests and four refresh attempts. Nothing in the retry rule needs those queries to throw: mobile's client already threw on every refusal, and the web hooks that pass `throwOnError` per call already paid the retries. Leaving the order alone was the other option, on the argument that the branch ships whole, and it was rejected because a per-commit deploy is the thing the ordering is for. It sits second, behind the commit that gives an error its status code, since the rule reads that status.
- Finding `retryable-4xx`: the rule gave up on every status from 400 to 499, and 408 and 429 are the two a later attempt can still satisfy, since there the server is asking the caller to wait rather than settling the question. `RETRYABLE_REFUSALS` holds them. Nothing reaches it today: the only rate-limited read is `GET /link-preview`, and `useLinkPreview` already sets `retry: false`. It still matters, because this rule is now the default for every query in three apps, so the next throttled read would lose its retry with nothing in the diff to say so. Leaving the blanket and naming the choice in the comment was the other option, rejected because a comment does not stop the bug. The client drops the response, so a retried 429 falls back to React Query's backoff instead of the server's `Retry-After`; the error interceptor the commit before this one registers still holds the `Response`, and is where that header would have to come from. It carries the status code and nothing else, since no caller asks for `Retry-After` yet.
- Finding `two-checks`: `refusalStatus` returned a status or `undefined`, and the caller tested for `undefined` again before the range check. One `isRefused` predicate says the same thing in one place, and drops a parse that reports failure as `undefined`.
- Finding `weak-test`: the test for a request that never reached the server called the rule directly with a `TypeError`, which every error passes, since `failureCount` 0 is under 3 for anything the rule does not refuse. The route throws from inside the fetch now, so a real rejection reaches React Query and the attempt count is what the test reads. The refusal and retry cases are tables, 400, 401, 403 and 404 against 408, 429 and 500, so both edges of the window and both carve-outs are pinned. Checked by emptying `RETRYABLE_REFUSALS`: the 408 and 429 cases fail.
- Finding `unused-seam`: the file threw away what `serveApi` returned and passed `throwOnError` per call, a shape no app ships. It takes `throwingOnRefusal` now, which configures the client the way mobile configures it, and mobile is the client every query in this rule runs under.
- ALL-1112's closing comment described the rule as it stood before this round, down to the sha and the request counts its tests assert. It was rewritten in place rather than answered with a second comment: the issue is closed, so nobody is mid-conversation there, and a correction left standing beside the version it corrects keeps the wrong one readable.
- Finding `shadowed-retry`: `useVisibilityContext` and `useAllianceMemberCount` set `retry: 1`, which overrides the default the rule installs, so a refusal there still cost a wasted request and a backoff. `useVisibilityContext` is the one that shows: its own comment says the whole form is gated on the query settling, and `GET /user/myvisibilitycontext` is behind `AuthGuard`, so a dead session sat on a spinner for a round-trip it could not win. Both take `retryUnlessRefused(1)`, keeping the budget each asked for. It is its own commit because neither hook is part of installing the rule, and the rule deploys without it. No test: `useVisibilityContext.test.ts` covers the pure `hasSettledSinceMount` today, and rendering the hook against a served API would be new scaffolding to pin behaviour `retryQuery.test.ts` already pins.
- The tests for a failed refetch keeping the data it had went into the reviewed commit itself, since that commit is what makes them pass. Same for the nit about the test passing a bare `2` as a user id. From findings `refetch-untested` and `magic-user-id`.
- Finding `plain-object-error`: the five queries declared their error as `Error`, but `@hey-api/client-fetch` throws the parsed refusal body, so a caller gets `{ message: "Internal server error" }`, or a bare `{}` when the refusal carries none. The option types now say `unknown`, matching `FriendRequestMutationOptions` further down the same file and `errorMessage` in `common/src/errorMessage.ts`, both of which already take an error as `unknown`. A test pins the shape so a later change to it is deliberate.
- Turning the refusal into a real `Error` was the other way to make that type true, and was left alone. Only these five queries would get the new shape while every other `throwOnError: true` call in the repo kept the old one. The place to do it once is the client's error interceptor, which still holds the `Response` and so can carry the status code `refusalMessage` asks for. Filed as ALL-1111 with the source fix.
- Finding `web-client-default`: `createClientConfig` sets `throwOnError: false` and only mobile overrides it, which is why a web call swallows a refusal unless it opts in per call. Filed as ALL-1111, not fixed here, for the same reason the source fix was left alone. The fourth, fifth and sixth reviews each asked for it again; the issue was already open each time, so it took that round's commit shas rather than a duplicate. The branch is rebased between rounds, so a sha quoted in the issue resolves only until the next one.
- Finding `fallback-asymmetry`: `ServedApi` now has two helpers whose tables behave differently, one replacing the file's routes and one layering over them, and only the doc comments said which. The layering one is `alsoServing` rather than `serving`, so the contrast with `throwingOnRefusal`'s "instead" is in the name. Making both layer was the other option and would have cost `throwingOnRefusal` its "only these routes exist" check in four test files.
- Finding `silent-unrouted-get`: the 500 routes for the friend reads moved out of the file's route table and into the two tests that want them, through a new `ServedApi.alsoServing`. That table is what makes `serveApi` throw `UnroutedRequest` for a request no test meant to make, and a route registered for the whole file gives that check up for every other test in it.
- Finding `no-success-test`: every test for the five queries served a 500, so nothing pinned what they return on a 200 once `?? null` and `?? []` came off. Each query now has a success case, reusing the `erroringQueries` table and serving the same body the refetch case seeds the cache with.
- Finding `alsoserving-resets`: `alsoServing` passed `throwOnError: false` outright, so a test calling it after `throwingOnRefusal` went back to the web client without saying so. It keeps whatever the client is already set to. Saying it in the doc comment instead was the other option, and leaves a test that combines the two checking the client it did not ask for.
- Finding `refusal-throws-global`: the flag `alsoServing` reuses is read off `client.getConfig()`, not off a module-level `let` that `serve` writes on every call. The client already holds it, and the module variable was a second piece of state outliving a test which, unlike `unrouted`, nothing reset.
- Finding `alsoserving-replaces`: the `alsoServing` doc comment says it replaces a table an earlier `throwingOnRefusal` or `alsoServing` installed. Its fallback is always the handler `serveApi` was built with, and "also" reads as additive. Layering over the last table served instead was the other option, rejected because no test wants it and it gives `throwingOnRefusal` two meanings depending on what ran before it.
- Findings `unused-throw-inherit` and `message-inaccuracies`, applied on the user's instruction to fix every finding: `alsoServing` serves with `throwOnError: false` again, reversing `alsoserving-resets`. No test in the branch calls it after `throwingOnRefusal`, so keeping the flag cost a config read and a test for a combination nobody used, and it kept one call's throwing while dropping its routes. The doc comment now says it answers the way web configures the client and replaces whatever an earlier call installed, so a test combining the two learns it where it hovers the call. The commit message and a test name stopped calling a 500 a refusal, the word `retryUnlessRefused` reserves for a 4xx it will not retry.
- These changes come from a review of "Tell the user when a friend action fails". The user asked for every finding to be fixed, including the nits, and for no REQUIREMENTS.md to change.
- Finding `duplicate-refusal-parse`: the friend actions' message helper repeated admin's `actionSaveErrorMessage`, parsing the `statusCode` `registerErrorStatus` puts on a thrown error before handing it to `refusalMessage`, with a looser schema. `thrownRefusalMessage` now lands ahead of that commit with admin as its first caller, so the second copy never exists. It was otherwise extracted two commits later, once both copies were in.
- Finding `double-tap-false-failure`: Accept and Decline stayed tappable until the lists they change reloaded, so a double tap sent a second PATCH that the server answers 404 "No pending request found" after the first went through. The next commit shows that 404 as a failure, so this lands first. A friend mutation now stays pending until the lists it invalidated have refetched, which keeps the web Friends tab's `processingIds` up until the answered row leaves. The web profile's Accept and mobile's Accept and Decline are disabled while one is pending. Mobile disables every received-request row while any one is answered, rather than tracking ids per row, since the wait is one request and one refetch. Making accept and decline idempotent on the server was the other option, and was left alone because it changes what the API answers.
- Finding `untested-disable`, from a review of this commit: a test renders `FriendRequestButton` with `accepting` and expects Accept disabled, so dropping the prop fails a test rather than bringing the 404 back. Mobile's Accept and Decline stay untested, since mobile has no component tests.
- Finding `forum-queries-same`: 79 other calls still read `response.data` with a `??` fallback, so a failed fetch reads as empty data. This commit covers the forum post and comment queries, the general update queries, my communities, and the search box. The rest are left alone rather than swept in one commit nobody can review.
- The fix at the source is `throwOnError: true` in `createClientConfig`, which would change every call in the repo at once, including the ones that read `response.error` and handle it. That deserves its own pass, ALL-1111, not a line in this commit.
- The two forum queries join the friend queries in `user.test.ts`. The general update and communities hooks get test files of their own covering the same two failures, and search one for a failed search.
- These changes come from a review of "Fail the forum activity, general update, communities and search reads", first titled "Fail the other queries a profile page and search read". The user asked for every finding to be fixed, including the nits, and for no REQUIREMENTS.md to change.
- Finding `communities-no-iserror`: `useMyCommunities` put a failed fetch into the query's error state and returned no `isError`, so no screen could tell a failed load from belonging to no groups. It returns `isError` now, beside `isLoading`. The screens that read it come in a later commit.
- Finding `message-scope-claim`: the title and last paragraph said every query here is one a profile page or the search box reads, but the general updates feed the home and General Updates pages and my communities feeds the groups, invite and community screens. Both now name the four surfaces.
- These changes come from a second review of "Fail the forum activity, general update, communities and search reads". The user asked for its one must-fix to be fixed and for no REQUIREMENTS.md to change.
- Finding `home-waits-on-retries`: the unread general updates query threw now, so React Query retried a 500 (1s, 2s, 4s on web), and both home screens show a full-page spinner until it settles. A general updates outage held the home page for about 7s on web, where it used to render at once. The query takes `retry: false`, so a failure settles on its one attempt as it did before on web, and the Try again button a later commit adds is the retry. Letting the home screens stop waiting on general updates was the other option, rejected because mobile sorts general updates in among the tasks, so updates arriving late would move the task the user is looking at. The General updates page's query keeps its retries, since the updates are the whole page there.
- These changes come from a third review of "Fail the forum activity, general update, communities and search reads". The user asked for its one should-fix to be fixed and for no REQUIREMENTS.md to change.
- Finding `mobile-loses-retry`: mobile configures its client with `throwOnError: true`, so there these reads already failed, and the unread general updates query already retried twice, holding the home page about 3s. `retry: false` stays, so mobile gives up those retries on purpose: a failure renders the home page at once without general updates, which come back on pull to refresh or the next refetch. The commit message now says the swallowed failures were web's, and what the missing retry changes on mobile.
- Finding `error-state-unrendered`: the failure replaces the empty state of the list it belongs to, rather than one error card for the whole Friends tab. A tab whose own query worked keeps showing its data, and the retry refetches only the list that failed.
- A failed friend status shows a refresh icon button where the friend button goes, following the repo's preference for an icon over a text button. The Friends tab uses a worded "Try again" under a sentence saying what failed, since an icon alone in an empty list says nothing.
- Mobile gets the same two states, for the parity `apps/AGENTS.md` asks for. Its list empty state goes through the FlatList's ListEmptyComponent, which already switches on the tab. Only web has a test, since mobile has no component tests.
- These changes come from a review of "Show a retry when friend data won't load". The user asked for its three should-fixes and its nit to be fixed and for no REQUIREMENTS.md to change.
- Finding `stray-status-retry`: React Query keeps a failed refetch's data and flags the query as errored, so on web a status that had loaded put the refresh icon beside a working friend button after a failed refetch, such as a window refocus. The icon and the list failures read `isLoadingError`, which holds only for a failure with no data. Mobile's friend action already checked for a missing status first and is unchanged.
- Finding `zero-count-on-failure`: a failed list counted as 0, so the Friends tab headers and the profile's friends pill said "0" above "Couldn't load this list." A failed list's count is left out and its label stays.
- Finding `silent-retry`: a retry refetches through the client's retry policy, 3 attempts on web and 2 on mobile, and the query stays errored until they settle, so an unchanged button sat on screen for seconds. The Try again button tracks its own `onRetry` promise and disables on web, or spins on mobile, until it settles, which covers any caller whose retry returns a promise without a flag per caller. The refresh icons read the status query's `isFetching`, since they are plain buttons.
- Finding `thin-retry-tests`: web tests cover the friend status failing and retrying, a status refetch failing on top of good data, a failed Received Requests list, the counts, and the disabled Try again. Mobile still has none, having no component tests.
- These changes come from a review of "Show a retry when friend data won't load". The user asked for its should-fix and nit to be fixed and for no REQUIREMENTS.md to change.
- Finding `mobile-iserror-mismatch`: mobile read `isError`, which stays true when a list that loaded empty fails a later refetch, so its empty text turned into a retry where web kept it. Mobile reads `isLoadingError` like web, so only a list or status that never loaded shows the retry.
- Finding `withcount-name-clash`: the Friends tab's label helper is `tabLabel`, since `@alliance/common/plural` already exports a `withCount` that formats "3 friends".
- These changes come from a review of "Show a retry when friend data won't load". The user asked for its must-fix to be fixed and for no REQUIREMENTS.md to change.
- Finding `retry-shows-empty`: React Query puts a query with no data back to pending when it refetches, which clears `isLoadingError`, so a retry brought back "No friends yet." and "Friends (0)", and the refresh icon vanished, until the retry settled. The failure flags read `failedToLoad` in `shared/lib/failedToLoad.ts`, true while the query has no data and has errored at least once (`errorUpdateCount`, which a refetch leaves alone), so the retry and its disabled or spinning state stay up through the retry. It is shared because web, mobile and the later retry commits read the same flag. The web tests hold the retry's response open and check what shows meanwhile.
- These changes come from a review of "Show a retry when friend data won't load". The user asked for its should-fix to be fixed and for no REQUIREMENTS.md to change.
- Finding `stale-failure-on-reopen`: `failedToLoad` reads `errorUpdateCount` off the cached query, so a list that failed and is opened again shows the failure while its new load runs, and Try again, which tracked only its own `onRetry` promise, stayed enabled through it. `LoadFailed` takes a `retrying` flag from its caller instead, the query's `isFetching` for the friend lists, as the refresh icons already did. This reverses `silent-retry`'s choice of a promise over a flag per caller: the promise only knows about fetches the button started. Showing the loading text during that reload was the other option, and would also clear the failure during a retry, which `retry-shows-empty` ruled out.
- Finding `messages-page-untouched`: the Messages page loaded messageable users from its own `useEffect` around `userListMessageableUsers()`, keeping the result only `if (response.data)`, so it kept the swallow the friend queries just lost. It reads `useMessageableUsersQuery` now, the hook every other screen already uses, which also puts it on the shared cache the friend mutations invalidate. It gets its own commit: the page is not one of the queries the earlier commits cover, and the swap stands on its own.
- The page kept `ProfileDto[] | null` for that list, where the hook gives `ProfileDto[] | undefined`, so the destructure defaults to `null`. `ConversationDetailPanel` takes `friends` as nullable and the page reads it with `?.`, and changing that prop is a bigger edit than this finding asks for.
- Finding `no-page-test`: `MessagesPage.test.tsx` fails the first messageable-users load with a 500 and checks the new-conversation picker offers the user once the retry succeeds. It runs the query client with `retryUnlessRefused(3)`, the retry `root.tsx` sets, and stubs the conversation hooks, which open sockets.
- Finding `errors-still-silent`, from a review of "Fail the forum activity, general update, communities and search reads": the forum activity, search, general updates, and communities hooks report a failure, and no screen read it, so each still looked empty. One commit per surface shows it, web and mobile together for the parity `apps/AGENTS.md` asks for.
- The friend lists' "Couldn't load … / Try again" block moved into a `LoadFailed` component on each platform, taking the sentence and the `retrying` flag as props, once the forum tab became its second caller. The forum notice passes whether either forum query is fetching.
- The forum tab merges posts and comments into one list, so either failing leaves it short. The notice sits above the list whenever one of them failed without data, rather than only in place of an empty list, so a profile whose comments failed does not pass off its posts as everything. It reads `failedToLoad`, like the friend lists, so a failed refetch that kept its data shows no notice and the notice stays up while Try again runs. Try again refetches only the query that failed. That rule lives in a `useUserForumActivity` hook in `shared/lib/user.ts`, beside the two queries, so web and mobile read one copy of it. On mobile the notice goes in the FlatList header, since `ListEmptyComponent` only shows when the list is empty.
- Search's hook runs its request from an effect, not React Query, so there was no refetch to hand a retry button. `useSearchResults` returns `retry`, which bumps an attempt counter the effect depends on and so re-runs the current query. Moving search onto `useQuery` was the other option, and changes debouncing, cancellation, and caching for a retry button.
- A failed search shows "Couldn't search." with Try again where "No results found" goes, on web in the dropdown and on mobile under the input.
- Both search boxes pass the hook's `loading` as `LoadFailed`'s `retrying`, which turns on as soon as `retry` runs. On web the dropdown hides the notice while the search loads. On mobile the notice stays up, with Try again spinning, until the search settles.
- Web home shows general updates in their own section only when there are some, with no empty state for a failure to replace, so the notice sits where the updates go. Mobile home counts general updates as tasks, so with nothing else to show, the notice takes the place of "No tasks to do right now"; otherwise it sits above the current task. On web, `useTaskActionsData` returns `generalUpdatesFailure`, the retry and its `retrying` flag when the updates failed and `null` otherwise, rather than an error flag, a refetch and a fetching flag the page would have to pair. The general update hooks return `isFetching` for that flag. On mobile the notice goes inside the view whose height `scrollToEnd` measures, so scrolling to the end still lands at the bottom of the body.
- The General updates page had no empty state either. Its notice sits above the list.
- These changes come from a review of "Show a retry when general updates won't load". The user asked for its two should-fixes to be fixed and for no REQUIREMENTS.md to change.
- Finding `notice-over-cached-list`: the notice showed on any error, so a failed refetch that kept its updates put "Couldn't load general updates." above them, where the groups screens leave a kept list alone. The general update hooks return `didFail`, read from `failedToLoad`, and every notice reads it. Web home's `useTaskActionsData` no longer turns an error into `generalUpdates: null`, so a failed refetch there keeps showing the updates it had rather than hiding them with no notice.
- Finding `failure-derivation-untested`: `useTaskActionsData.test.ts` fails the unread load, holds the retry's response open, and checks that the failure and its `retrying` flag stay up while home keeps rendering. Writing it showed the retry put the query, which had no data, back to pending, as `retry-shows-empty` found for the friend lists: `isError` cleared, so the notice vanished and both home screens went back to their full-page spinner until the retry settled. Reading `didFail` keeps the notice up, and both home screens stop counting general updates as loading once they have failed. The General updates page test holds its retry open the same way and checks that Try again is disabled.
- A second review of "Show a retry when general updates won't load" found two should-fixes, and the user asked for both to be fixed with no REQUIREMENTS.md change. Finding `mobile-empty-claim`: mobile home showed the notice above "No tasks to do right now", which claimed there was nothing to do when general updates had not loaded; the notice now replaces it, matching the groups screens. Finding `untested-cached-keep`: `useTaskActionsData.test.ts` now checks that a failed refetch keeps the updates web home already had.
- The groups screens read `didFail`, which `useMyCommunities` returns from `failedToLoad` like the general update hooks, so a failed refetch that kept the list leaves it alone. Finding `web-retry-flash`: reading `isError` instead let Try again put the query, which had no data, back to pending, and web Manage groups showed the empty state and its assignment button until the retry settled. With `didFail` the notice stays up with Try again disabled, and the mobile screens stop counting the groups as loading once they have failed. The empty state there was not just wrong but offered "Request assignment", which asks staff to place someone who may already be in a group, so a failure hides it. Web's Manage groups page and mobile's manage screen put one notice above both group sections and drop both sections' empty text. The mobile Groups tab has only an empty screen to replace, and shows the notice there. CommunityPage falls back to Manage groups when it has no community, which puts the same notice on screen there.
- The invite forms, the invites pages, and the comments filter also read `useMyCommunities`, where the groups only fill a picker or a filter. They are left alone: a failure there leaves out an optional choice, not a claim about the user's groups.
- Finding `plus-button-unlabeled`: the Messages page's new-chat button was a bare Plus icon, so its test reached it through the header's markup. It takes `title="New chat"`, which gives it a tooltip and an accessible name; the shared web `Button` passes `title` through and has no `aria-label` prop. The test finds it by that name.
- Finding `silent-empty-picker`: when messageable users failed to load, the new-conversation picker on the web Messages page and the mobile new-message screen showed nobody. Both now read `failedToLoad` on `useMessageableUsersQuery` and show `LoadFailed` with Try again in place of the picker, refetching the query and passing its `isFetching` as `retrying`. The mobile screen is included for web-mobile parity. With the picker gone, only a `?to=` link can pick a recipient, and one recipient makes a direct conversation, so no group gets a title that is missing names. `ConversationDetailPanel` takes the failure as one `recipientsFailure` object (`{ onRetry, retrying } | null`), null in `existing` mode like the panel's other new-only props. On mobile the notice sits in the "To:" row, wrapped in a `flex-1` View like the picker it replaces, since a React Native row child doesn't shrink and the label would push the text past the row's border. The page test holds the retry's response to check the notice stays up, Try again disabled, until the list arrives. The group-info "add members" lists on web and mobile read the same query and still show an empty list when it fails; they are left for a separate change.
- These changes come from a review of "Show a retry when the people you can message won't load". The user asked for its two should-fixes and its nit to be fixed and for no REQUIREMENTS.md to change. The nit corrected the commit message only.
