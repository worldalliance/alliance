- The fix comes from the review of "Notify the addressee when a declined friend request is sent again", finding `cancel-leaves-notif`. The user asked for every finding to be fixed. When A cancelled a pending request, `removeFriend` deleted the row and left B's "wants to be friends" notification unread, pointing at a request that no longer existed.
- `removeFriend` now marks the deleted row's `sentNotif` read, through `setRead`, the same call accepting or declining makes. I marked it read rather than deleting it, to match those two paths. On an accepted friendship the notification is already read, so unfriending changes nothing there.
- The `acceptedNotif` ("B accepted your friend request") is left alone on unfriend. It still describes something that happened.
- `removeFriend` loads the pair's row with `findOne` and deletes it by id. `Friend` is unique on the pair, so there is at most one row, and the pair condition is written once instead of again in a raw delete. From the review of this commit, nit `one-row-per-pair`; the user asked for every finding to be fixed.
- The e2e tests check that B has one unread request notification before the removal, so they can't pass if none was created, and cover both the requester cancelling and the addressee removing the request. From the same review, nit `test-precondition`.
- The commit sits right after the one it came from. Nothing later in the branch depends on it.
