- Found no existing precedent in the codebase for referencing one specific action by a hardcoded/configured numeric id (checked for constants, env vars, config files — none exist). Given the request names one specific action (not a category), added a local `welcomeQueueActionId = 9` constant with a comment linking to https://thealliance.org/actions/9, rather than introducing a new boolean entity column for a single action.
- Initially misread the request as two distinct actions (action 9, and a separate "sign your contract" action identified by the `isContractSigningAction` flag) and built the query to exclude users with a signed `contract_event`. The user corrected this: action 9 is itself the "sign your contract" action; its contract-signing requirement was removed, so the query is independent of contract-signing status. Removed the `contract_event` join/exclusion entirely — the query now only requires completing action 9.
- Kept the existing "no staff comment yet" exclusion and staff-like filter/count unchanged — the request only changed which population enters the queue, not the already-welcomed exclusion logic.
- Renamed `UnwelcomedSignedContractMember(Dto)`, `findUnwelcomedSignedContractMembers`, and `getUnwelcomedSignedContractMembersAdmin` to `WelcomeQueueMember(Dto)`, `findWelcomeQueueMembers`, and `getWelcomeQueueMembersAdmin` — the old names describe signed-contract members, which no longer matches the new population. Kept the `GET /actions/welcome-queue` route path as-is.
- Dropped the `signedAt` field from the DTO/type and the "Signed" column from the admin table — signing status is no longer part of this queue's criteria. Sorting now uses `completedAt`.
- Updated `apps/admin/src/pages/WelcomeQueuePage.tsx` copy and the empty-state text to describe the new criteria. Used a neutral "Action" link label instead of naming the action, since its current display name isn't necessarily "sign your contract" anymore and isn't returned by the endpoint.
- Regenerated `shared/client/{types,sdk}.gen.ts` via `bun run gen-api` against the running local dev server, per `apps/AGENTS.md`.
- Verified against the local dev server: hit `GET /actions/welcome-queue` with a minted admin JWT and cross-checked the returned count against a hand-written SQL query using the same criteria; both returned 342.
