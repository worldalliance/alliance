- The change comes from the review of 890cfd771 ("Refuse a friend request to an existing friend"), nit `crossing-request-accepts`. The user asked for every finding to be fixed.
- When B sends A a request while A's request to B is pending, the server accepts A's request instead of answering 409. Both users have asked, so accepting is what B means. It goes through `updateFriendRequestStatus`, so A gets the "accepted" notification, the notification B got for A's request is marked read, and messaging gets the `FriendsAccepted` event, same as tapping Accept.
- The endpoint answers 201 with the caller's friend status toward the target, the same body `GET /user/myfriendrelationship/:id` returns, so a client can tell "request sent" from "friends" without asking again. The controller reads it back through `getRelationshipStatus` instead of building it from the saved row, so the two endpoints can't disagree. This came from the review of "Refetch the friend status after sending a friend request", finding `guessed-status`: the clients wrote "request sent" on success and refetched after, so for one round trip after an accept they offered to cancel, and cancelling deletes the friendship. The user asked for every finding to be fixed.
- Only a stale screen reaches this, since the clients show Accept once a request is received. Mobile builds from before the client commit keep showing "request sent" until the friend status refetches.
- A send that accepts the other user's pending request logs `FriendRequestAccepted` to PostHog, with `requesterId` set to the other user as the Accept endpoint does, instead of `FriendRequestSent`. The controller picks the event from the status of the row `createFriendRequest` saved. Reading the status back through `getRelationshipStatus` afterwards found the wrong row when a pair had two, and could see another user's change made in between. This came from the review of eeb87668b ("Refuse a friend request to an existing friend"), nit `analytics-on-accept`, which the user asked to fix. Picking it from the saved row came from the review of b2a612dcc, finding `analytics-from-reread`.
- The endpoint declares its 409 in the OpenAPI spec, so the generated client types it. From the review of eeb87668b ("Refuse a friend request to an existing friend"), nit `undocumented-409`.
- That review also flagged pairs left with two rows by the old code, which a send could turn into two accepted rows. The user believed there were none; staging (pruned production data) had 988 friend rows across 988 distinct pairs on 2026-09-19, so this commit doesn't clean them up.
