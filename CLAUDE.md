@AGENTS.md

## Memory

Don't write to the auto-memory system (`~/.claude/projects/.../memory/`). Persistent guidance goes in an `AGENTS.md` or under `.claude/skills/`, where it's visible to everyone reading the repo instead of being a hidden rule — create a new file there if none fits.
