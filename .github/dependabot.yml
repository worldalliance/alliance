version: 2

updates:
  # uniwind alone. It is pinned exactly, so nothing else walks it forward and
  # this PR is the bump's only prompt.
  - package-ecosystem: bun
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 1
    allow:
      - dependency-name: uniwind
