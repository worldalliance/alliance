import christina from "../assets/redesign/avatar-christina.webp";
import kanishk from "../assets/redesign/avatar-kanishk.webp";
import nihar from "../assets/redesign/avatar-nihar.webp";
import xuijin from "../assets/redesign/avatar-xuijin.webp";

/**
 * Faces inside the action mockups on the commitment screen. Bundled rather than
 * drawn from the live roll: a mockup that reshuffles on every load stops being
 * the same picture twice. The agreement's own faces are real members, via
 * `useSignupFaces`.
 */
export const MEMBER_FACES = [xuijin, christina, nihar, kanishk];
